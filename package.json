{
  "name": "portbill",
  "version": "3.14.0",
  "private": true,
  "description": "Dev-only tooling manifest for portbill. The app itself has zero runtime dependencies — no build step, no bundler.",
  "scripts": {
    "lint": "eslint .",
    "lint:css": "stylelint \"styles/*.css\"",
    "test": "node tests/run-all.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-sonarjs": "^4.0.0",
    "globals": "^15.0.0",
    "stylelint": "^17.0.0",
    "stylelint-config-recommended": "^18.0.0"
  }
}
